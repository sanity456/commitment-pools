# Commitment Pools — human wallet acceptance

Status: **both the one-round and the subsequent two-round human both-pass lifecycles are complete**. The [two-round completion record](frontend/verification/human-consent-2r-completion-2026-09-07.md) closes the latest run at `2026-09-07T12:50:44.137Z`: A and B passed 2/2, both received exactly 100 wei through independently finalized and credited native transfers, and remaining credits and fees are zero. No further wallet transaction is needed to close that run. Unobserved manual branches remain explicit coverage limits below; this is not exhaustive human acceptance or an independent security certification.

The earlier 60-minute one-round retry has eleven successful finalized parent receipts: creation, both joins, activation, both complete source captures, both passing proofs, settlement and both withdrawals. Both stored proof timestamps fell inside the original round, and execution/finality were independently checked. B's saved-history refresh/reload and downloads passed in that trial; [A's final local refresh/reload and exports](frontend/verification/human-wallet-a-final-2026-09-07.md) also match its independent evidence. See the [historical one-round record](frontend/verification/human-wallet-60m-2026-09-06.md) for that separate scope.

Human login/rejection, account/network resets, two-way populated Activity isolation, bounded focus/Refresh/source re-consent checks and the [first trial's full underfilled-pool refund](frontend/verification/human-wallet-2026-09-06.md) are also recorded. A script signing an ephemeral account is not this test. Use Studionet only for transactions, with the user's own approvals at every wallet popup. Switching away is a read-only safety check: never send a transaction on the other network. Never request, export or record private keys or session credentials.

## Release and actors

September 7 follow-up: the [human creation-rejection test](frontend/verification/human-wallet-rejection-2026-09-07.md) passed. The exact request was saved as cancelled without a hash or linked transaction, survived refresh/reload, and left the prospective pool absent and tested balances/credits/statistics unchanged. This closes the rejection gap noted in the earlier immutable lifecycle snapshot, but does not claim a deliberate successful retry of that same unsigned creation. The later network-canceled proof and its successful replacement are separate evidence.

- Record the tested commit, exact Vercel URL, browser/extension versions and UTC start time.
- Current versions are now documented in the owner's [September 7 screenshots](frontend/verification/wallet-environment-2026-09-07.md): Chrome `152.0.7977.83` (64-bit), with `152.0.7977.76` shown before the update, and MetaMask `13.46.1`. These were collected after the wallet trial; they do not establish the exact version pairing for every earlier transaction. Record the versions at the start of the remaining human checks.
- Confirm chain 61999, core `0x7279B4A7821c96489c0b086021F3E6944d343bFB` and helper `0xb21aa001F13B3c0f3940CC2352988D28b4E40f1D`.
- Confirm two distinct participant wallets A and B with enough test GEN. Prior Dispute Court approvals are not approvals for this product.
- Initial trial: 100 wei each (`0.0000000000000001 GEN`), two participants, one one-hour round, 15-minute formation. Only A joined before formation ended; the full stake was subsequently returned through the independently verified formation-refund path. The owner subsequently approved a separate 60-minute formation retry with the other terms unchanged; follow its [current private record](frontend/verification/human-wallet-60m-2026-09-06.md). Review the exact terms and snapshotted fee before creation. The creator must separately join if participating.
- All-fail policy is `refund_minus_fee`: at a 500 bps snapshot, two failed 100-wei participants are expected to receive 95 wei each and the fee recipient 10 wei; this all-fail outcome was not observed in the human trial. The both-pass result is now observed: each received exactly 100 wei and the fee was zero.

## Wallet and recovery checks

1. Open the intended release in Chrome with the supported injected EVM wallet. Confirm there is no email registration/password form.
2. Reject the first login signature. Verify the user remains signed out and sees a cancellation message. Approve a fresh origin/product-bound login signature; confirm no transaction or value transfer was requested.
3. Reload and restore the same wallet session. Prepare an unsent draft and test network and account changes separately. In MetaMask, keep the Commitment Pools tab active, select the network control beside that site's connection, and change that connection to Ethereum without sending anything. In the user's September screenshot, this was the `G` dropdown in the bottom connection bar; other versions expose it through a connected-site icon. Changing the portfolio's network filter alone does not test the site's connection; MetaMask manages networks per website ([official instructions](https://support.metamask.io/configure/networks/how-to-change-networks/)). Record the site-specific network shown. Before reloading, verify private views and pending form approvals clear and writes stay blocked until a matching login and fresh state read succeed. Return the site's connection to Studionet and sign in again, then prepare another unsent draft and repeat the account-change check with the other participant wallet. Do not mark either check passed solely from a reported switch or a draft cleared by a browser reload.
4. Prepare creation, reject its wallet transaction, and confirm Activity records an unsigned cancellation with no hash. Review and retry deliberately; preserve the actual transaction hash and final execution result.
5. During a subsequent form, switch browser focus and refresh chain data. Unchanged recorded terms must retain drafts and evidence review. Changed wallet, pool, round or recorded terms must require fresh consent. Never repeat an uncertain transaction; reconcile its hash first.

## Full pool lifecycle

1. Approve a new pool with a unique clearly labeled test ID. Export the stored creation record, `terms_hash`, `join_deadline`, `activity_starts_at` and `activity_ends_at`.
2. A and B each review terms and approve the exact 100-wei join. Verify both receipts are finalized **and successful**, and the recorded cohort/stake values agree.
3. Wait for the stored chain formation deadline. Confirm activation is denied before it and accepted on/after it, but before the first round closes. Do not change a local clock or infer eligibility solely from elapsed wall time.
4. Use a deliberately low-sensitivity, public source and `source_verified` rules. Capture and review the complete text for each wallet, recording URL, normalized byte count, digest, capture ID and transaction. Submit each proof during its stored round window, recording exact proof/nonce inputs, attempt payloads and actual verdicts. Do not claim a technical source-content fixture proves a real person's off-chain work; if using real progress notes, the rules must explicitly bind the participant and round.
5. Once all participants are terminal or the stored activity deadline has elapsed, approve settlement. Record the actual settlement payload, fee, per-wallet credits and conservation total. If a model/source call fails, retain the hash/result, retry only within the original attempt/time limits, and use deterministic deadline settlement if needed.
6. A and B separately approve withdrawal of their actual available credits. Verify each successful parent and each finalized, credited native child: sender, intended recipient and exact wei. A successful withdrawal call alone is not proof of delivery.
7. Reload Activity and export the pool, attempts, settlement and delivery evidence. Reconnect each wallet and confirm private data remains isolated. Redact only secrets, not failures or inconvenient outcomes.

## Completion record

At the earlier September 7 rejection and A-history checkpoint, live changed-pool/round/terms combinations remained incomplete. A's post-withdrawal Activity refresh/reload and actual exports passed on the local UI: ten A-only requests matched both historical trials, including the 100-wei payout, and the pool export matched all seven pool-scoped core calls. The subsequent draft and two-round checks below add bounded live consent and round-transition coverage; they do not retroactively extend the earlier checkpoint or claim exhaustive human permutations.

UI follow-up `CP-HUMAN-UX-01` was implemented in the [September 7 UI pass](frontend/verification/ui-polish-2026-09-07.md): a fresh zero balance no longer prompts another withdrawal, and payout delivery is still verified separately in Activity. That pass had 121 frontend tests, including seven credit-guidance checks. A's subsequent authenticated local check confirmed the zero-credit guide and disabled withdrawal before and after reload. The owner then approved [the protected preview release](frontend/verification/private-preview-2026-09-07.md), now deployed from local commit `363d79e1245725c57ec1c874f0b0675cf0dca1a6`. Its signed-out Chrome/HTTP checks passed. The subsequent [human Wallet A check on that new origin](frontend/verification/private-preview-wallet-a-2026-09-07.md) also passed: user-approved sign-in, session recovery after home and direct-pool reloads, ten identical saved Activity cards after reload/refresh, and zero-credit guidance with withdrawal disabled. The recorded environment for this check is Chrome `152.0.7977.83` (64-bit) and MetaMask `13.46.1`; the earlier lifecycle retains its separate version limitation. No new Wallet B/account-switch/network-switch coverage, GitHub push, payout or public CI result is implied.

The owner subsequently authorized the [consent regression/private-preview follow-up](frontend/verification/consent-regression-2026-09-07.md). The complete application suite now passes **153 tests**, including 32 new stored-evidence-based checks and 127 combined context-change cases. In the real Chrome session, switching between both closed human pools cleared stale details and loaded each pool's correct terminal state. These are bounded browser and automated checks, not a human checked-consent or multi-round lifecycle. At that checkpoint the latter remained incomplete, and no new on-chain transaction had been initiated for that coverage.

Save the exact inputs, stored timestamps, transaction hashes, output payloads, expected versus actual errors/verdicts and independent payout checks at an approved immutable commit. Public demo access has been approved and verified; GitHub source/CI/evidence publication remains deferred until the owner authorizes it. Repository and preview protection do not make on-chain records private. Record rejected prompts as human observations, never fabricate rejected transaction hashes. Retain honest notes for unsupported wallet types and failed/retried steps; do not mark unobserved branches passed.

The [subsequent unsigned creation-review checks](frontend/verification/human-consent-draft-2026-09-07.md) passed six bounded UI observations, including cleared acknowledgement after editing the schedule, rule text and pool identity. Those are draft-dialog tests, not live pool/proof or round-transition coverage. At that historical checkpoint a two-participant, two-round Studionet proposal was prepared but unpublished and awaiting exact-terms approval; no wallet prompt or transaction was initiated by the draft checks.

The owner-approved [live two-round test](frontend/verification/human-consent-2r-live-2026-09-07.md) is now complete: eleven successful finalized parent transactions cover creation, two exact 100-wei joins, activation, four passing proofs, settlement and both withdrawals. Both native children were independently verified credited/finalized, returning 200 wei in total with zero fees and remaining credits. Stored chain records establish every proof window and settlement eligibility; exact inputs, nonces, outputs and payout linkage are retained.

On the forming pool, explicit acknowledgement enabled joining, unchanged Refresh retained consent while blocking writes during reads, and switching to an old pool and back cleared consent. Both joined positions survived reload. Human wallet switches cleared the old session/workspace before agent reloads, and fresh matching-wallet logins restored the correct positions. A's natural round-one-to-two transition produced an empty new form; reusing its historical capture required fresh review. B's separate round-two form after login also required fresh unchecked review. Terminal progress removed proof controls, settlement allocated exact credits, and both final zero-credit views disabled withdrawal. Saved payout cards matched the independently verified children.

One A round-two request was canceled by the network after three recovery attempts, with no VM verdict or recorded proof attempt. Complete pool/cohort reconciliation showed no state change before one deliberate replacement with a new nonce passed. Both request histories remain visible; the underlying runner/provider cause is not established. This is not the same case as the earlier user-rejected unsigned creation. The current evidence-only closeout did not rerun the complete suite, push, change visibility or deploy.

Remaining coverage limits: no live early-activation rejection transaction, human all-fail lifecycle, deliberate retry of the separately rejected creation, or exhaustive manual consent permutations is claimed. Stored-time and consent automation retain their separately documented scopes. This human run used the protected preview, not a real-wallet login on the public alias, and its final saved-card checks are not a new full reload/export audit of every historical entry. Determine any additional required coverage from the actual program rules before scheduling more tests; do not automatically repeat the completed lifecycle.

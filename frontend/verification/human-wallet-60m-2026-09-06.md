# Private human wallet trial — 60-minute retry

Status: the successful two-wallet lifecycle is verified end to end. Both wallets passed; settlement conserved 200 wei with zero fee; A and B each received their full 100-wei stake through separately finalized, credited native transfers. Both current credits are zero. Eleven exact successful finalized parent receipts and both native children are verified. B's post-payout refresh/reload, isolated saved history and both downloaded exports passed their recorded checks. Full human acceptance remains partial: see the remaining coverage and UI follow-up below. This is Commitment Pools only. Keep the repository and protected preview private; on-chain terms and evidence remain public.

The owner replied `yes` to a new pool with a 60-minute join window, 100 wei per wallet and all other terms unchanged. This is a separate trial from the completed underfilled-pool refund at local evidence commit `60854384c24a0a896b2ffbbb3497ebb29ce9cfca`. No new evidence was pushed, no deployment was changed and the first pool was not reused.

- Preview: `https://commitment-pools-studionet-28cvvvtta-sanity3.vercel.app/`
- Deployed app source: `ace5d3f78968dc8bf06a333f2c65c1ee37baee6e`
- New pool ID: `human-wallet-60m-20260906-131207`
- Exact inputs, preflight values and ongoing receipts: [machine-readable record](human-wallet-lifecycle-60m-2026-09-06.json).

## Creation request

At 2026-09-06 13:12:58 UTC, read-only checks verified Studionet 61999, core protocol v3, the same owner and 500 bps fee, sufficient public native balances for both 100-wei stakes, and zero current contract credit for both actors. The existing app session showed `0xab99…bad4`. Only the pool ID and formation duration changed from the first trial; title, promise, rules, source-verified mode, stake, 2/2 cohort, one round and one-hour round length are identical.

At 13:13:34 UTC, the review dialog displayed the exact approved inputs and the unchanged failure consequences: forfeited stakes go to successful participants minus fees, all-fail outcomes return stakes minus fees, and underfilled formation allows a full refund. The source remains a technical public-page fixture, not a real-world work or identity claim.

At 2026-09-06 13:14:46 UTC, checked the review consent and clicked `Confirm & open wallet` once. The app entered `Waiting for wallet / finality…` with controls disabled. The human was asked to **approve**, not reject, this zero-value creation request. A stale payout-success toast for the first trial was not treated as evidence of the new request.

After the human replied `done`, A's saved Activity displayed the distinct new creation hash `0x3975f7f6ce89745a89678bb40394849062ea7627daa090b172301513bb547e3c`. At 2026-09-06T13:22:22.744Z, public RPC checks independently verified the exact A sender, core target, all 11 creation arguments, zero value, FINALIZED status and successful execution. The full calldata, return-code-0 output payload and stored pool are retained in the JSON record. Recomputing the canonical published-terms SHA-256 matched `f822901b2e7587245dd33e709f79940353769c33d94961579b67cb12c330495d` in both storage and the creation return payload.

The stored chain creation time is `2026-09-06T13:15:14.337719+00:00`. Formation ends and activity starts at Unix `1788704114` (`2026-09-06T14:15:14Z`); the one-hour activity ends at Unix `1788707714` (`2026-09-06T15:15:14Z`). These values prove the approved 3,600-second formation and 3,600-second round from stored chain data, not elapsed local-clock time. The pool was forming with zero participants, zero stake collected, the agreed 500 bps fee and `refund_minus_fee` policy. A fresh `can_join` read returned true.

## Wallet A join request

At 2026-09-06 13:23:44 UTC, the app showed the exact new pool, A's signed-in label and the unchanged 100-wei stake (`0.0000000000000001 GEN`). Reviewed the published rules, schedule and refund policy, checked the exact-stake consent, and clicked `Confirm terms & stake` once. Account, Refresh and transaction controls became disabled. The request was `join("human-wallet-60m-20260906-131207")`, from A to the core, with exactly 100 wei.

After the human replied `done`, the app initially reported acceptance for `0xb32db97ed088c0c0ac82d56c43465fbbfefbb1d8a8adcd8779c175dccbfaf4b1`; acceptance alone was not counted as success. At 2026-09-06T13:26:43.762Z, public RPC checks verified the exact sender, target, pool ID and 100-wei value, FINALIZED status, successful execution and return-code-0 payload. Stored participant `joined_at` is `2026-09-06T13:25:26.048257+00:00`, matching the receipt and preceding the stored join deadline. The pool now has one participant and exactly 100 wei staked; terms hash and all stored deadlines remain unchanged. The app subsequently showed the same successful finalized receipt, A's active participant position and 1/2 cohort, removed join controls and re-enabled account/Refresh controls.

At the same read-only checkpoint, `can_join` returned true and B still held 980 wei with zero contract credit, sufficient for B's agreed 100-wei stake. This does not establish which wallet is currently selected. No new transaction is pending; the human must switch the account connected to this site to B on Studionet before a fresh B login and separately approved join.

## Switch to B and fresh login

After the human replied `done` to switching the connected account, at 2026-09-06 13:28:48 UTC the app showed `Wallet or network changed. Sign in again to continue.` A's signed-in label, participant position and private credit view had cleared without a browser reload. Credit showed `Connect wallet to view`; withdrawal was disabled. This confirms the observed session reset, not yet B's new authenticated identity. The configured Studionet banner alone is not proof of the actual wallet chain.

Clicked `Sign in with wallet` once. At 13:29:14 UTC, the app showed `Checking wallet session…`, with sign-in and Refresh controls disabled. The reviewed login implementation requests an origin/product-bound `personal_sign` message after validating wallet and Studionet chain, then verifies the returned session. The human must approve only that login message in B. No B stake transaction was opened, no successful B login is claimed yet, and the MetaMask extension was not inspected or controlled.

After the human replied `done` to the login signature, at 2026-09-06 13:31:11 UTC the app showed `Signed in · 0xe6e7…5c43`, restored the exact 60-minute pool and showed B as not a participant with zero credit. The join consent was unchecked. A remained visible only as a public cohort member, without the `you` marker. This is a fresh authenticated app-session observation; the full exact B sender will also be checked against the actual signed join receipt. No authentication secrets were inspected or recorded.

## Wallet B join request

At 2026-09-06T13:32:15.941Z, fresh public reads verified Studionet 61999, the correct pool, unchanged terms hash and stored deadlines, exactly one participant (A) and 100 wei staked. B was absent from the complete participant list, still held 980 wei and had zero contract credit. The live `can_join` method returned true; no local-clock assumption was used for eligibility.

At 2026-09-06 13:32:33 UTC, reviewed the public immutable rules, exact 100-wei stake (`0.0000000000000001 GEN`), schedule, 5% forfeiture fee and refund policy. Checked B's fresh exact-stake consent and clicked `Confirm terms & stake` once. Account, Refresh, consent and stake controls became disabled. The request was `join("human-wallet-60m-20260906-131207")`, from B to the existing core with exactly 100 wei.

After the human replied `done`, the app showed successful finality for `0x14f4d61114fcdf8400b2ca2e8848b7c18feac86a5d5a2cec9716fb318fc98f7d`. At 2026-09-06T13:36:30.884Z, public RPC checks independently verified the exact B sender, existing core, pool-ID argument, 100-wei value, FINALIZED status, successful execution and return-code-0 payload. Stored B `joined_at` is `2026-09-06T13:34:26.198806+00:00`, matching the signed receipt. Both participants joined before the stored deadline. The complete cohort contains only A and B, each with 100 wei, exactly 200 wei total, unchanged terms/deadlines, and zero current credit. B's native balance is now 880 wei.

The live pool remains `forming`, with empty `activated_at`; the stored round opens at Unix `1788704114` and closes at `1788707714`. `can_settle` is false. `can_join` is also false because the pool is full; this does not prove that the formation deadline has elapsed.

At 2026-09-06 13:38:00 UTC, B's complete Activity page retained exactly two own requests before and after `Refresh saved history`: this new join and B's earlier underfilled-pool activation. No A requests appeared, despite the optional current-wallet filter being unchecked. The app confirmed refresh did not resend transactions. Returned through the exact new pool link: B remains signed in, 2/2 cohort, zero credits, and no activation control yet. This is observed UI history isolation/persistence, not comprehensive backend/API certification.

## Activation preflight and request

The human returned with `done` at the post-start checkpoint, observed 2026-09-06 14:16:28 UTC. B's signed-in session remained, the exact pool had 2/2 participants, and `Activate or enable refunds` was visible. Refresh preserved the matching session and pool. This was a return from the scheduled wait, not an activation approval. No background reminder had been scheduled.

The first non-broadcast write simulation, without an explicit timestamp, failed with `[EXPECTED] Formation remains open until 1788704114`, RPC code -32000 and user-error result code 1. It was not counted as current-time eligibility evidence. The inspected upstream Studio source passes an optional simulation datetime into the write path; without it, no datetime is supplied to GenVM. Ordinary signed transactions instead supply their stored `created_at`. See the immutable upstream [simulation endpoint](https://github.com/genlayerlabs/genlayer-studio/blob/c94072951e483510329670aa427fba3fa6944f45/backend/protocol_rpc/endpoints.py#L1553) and [node execution path](https://github.com/genlayerlabs/genlayer-studio/blob/c94072951e483510329670aa427fba3fa6944f45/backend/node/base.py#L622). This source explains the observed behavior, but the exact hosted Studio build was not identified.

At 2026-09-06T14:22:06.093Z, two fresh non-broadcast `gen_call` write simulations used timestamps derived exclusively from the finalized stored `join_deadline`: at `1788704113` (`14:15:13Z`) activation returned the exact expected formation-open error; at `1788704114` (`14:15:14Z`) it returned `active` with two participants and unchanged terms. The complete requests and actual raw/decoded outputs are in the JSON record. A subsequent read confirmed the real pool was unchanged and still forming. These are explicit stored-time boundary simulations, not fabricated live receipts or a change to the real chain clock.

Diagnostic handling note: the initial uncaught SDK error emitted excessive upstream debug metadata to tool output. None of the validator configuration, private material or full contract snapshots was copied to repository evidence. Follow-up probes catch errors and retain only relevant, allow-listed execution and input/output fields.

At 2026-09-06 14:22:27 UTC, rechecked the exact pool and B's label, then clicked `Activate or enable refunds` once. Account, Refresh and activation controls disabled. The real pending request is `activate_pool("human-wallet-60m-20260906-131207")`, from B to the existing core with **0 wei**. The old successful B-join toast/hash is not activation evidence. No activation hash or successful stored state transition has yet been observed; do not duplicate this request.

After the human replied `done`, the app initially reported the activation as accepted. At 2026-09-06T14:26:30.385Z, independently verified `0x2f2a97fb91e1ecad2d780ac641b8fe33d9441a51a3cf0f19719603b4335dbad5`: exact B sender, existing core, pool-ID argument, zero value, FINALIZED status, successful execution and complete return-code-0 output. The stored pool is now `active`, with no activation failure and `activated_at = 2026-09-06T14:24:49.301241+00:00`, matching the actual signed receipt. Its Unix timestamp `1788704689` is inside the stored `[1788704114, 1788707714)` activation window. Both participants still have 100 wei and zero completed rounds; the terms and one-round closing deadline did not change. The app subsequently displayed the same successful activation and B's empty proof form.

## Wallet B public-source capture

The same read-only checkpoint verified helper protocol v3, its binding to this core, 6,000-byte source limit, public-text normalization policy and `funds_accepted = false`. At 2026-09-06 14:27:13 UTC, filled the visible `Public source URL` with `https://example.com` and clicked `Capture source on-chain` once. The request is `capture("https://example.com", "2ed13b21-2dfe-4c23-a61e-506e3e5638b5")`, from B to the existing evidence helper with zero value. The generated recovery ID was read from the visible recovery disclosure, not hidden app state.

The app shows `Capturing…`, with account, Refresh and proof controls disabled. This capture records public permanent text; it is not a proof verdict or another stake. No capture hash or text is claimed yet. The old activation-success toast/hash is not capture evidence. B's proof text remains empty and no proof was submitted.

After the human replied `done`, the app displayed the distinct capture hash `0x8aac5ec758655b2a31a82b5e22c5db293534e23e782b90719d0ffeab283301cb`. At 2026-09-06T14:41:25.011Z, independently verified its exact B sender, helper target, URL, recovery ID, zero value, FINALIZED status, successful execution and return-code-0 payload. The complete stored snapshot matches the decoded return after normalizing the SDK's numeric representations. Its stored capture time, Unix `1788705240`, matches the actual transaction time `2026-09-06T14:34:00.638801+00:00`. The full 127-byte normalized UTF-8 source was reviewed without truncation and independently hashed to `8c1e8564424fdb68b8b7bdff3e16173a2e3599e9b71620637251486c5c4d5ed6`. The complete text is retained in the JSON; it supports both technical fixture facts. A first read-only assertion compared numeric view fields against string-normalized ABI fields; correcting that comparison did not require a repeat transaction or product change.

## Wallet B proof draft, consent recovery and request

Prepared this exact statement only after reviewing the complete source:

> For this sandbox round, I checked https://example.com. The page has the heading Example Domain and states that the domain is for use in documentation examples without needing permission. This is a public-page verification exercise, not a claim of real-world work or identity.

Between 14:41 and 14:43 UTC, checked source-review consent and clicked the app's `Refresh`. During loading, the unchanged draft, source, digest and checked consent remained, while proof submission was disabled. After fresh state loaded, all remained unchanged and proof submission re-enabled. Editing the visible source to `https://example.com/` then cleared the capture and consent and disabled submission, without losing the proof draft. An initial attempt to fill an empty string did not visibly change the controlled field and was not counted as a reset test.

Clicked `Load saved capture` with B's existing recovery ID. This was a read-only load, not a new capture transaction. The original exact URL, full source and digest returned, but consent was unchecked and proof submission stayed disabled. Reviewed the full recovered text again and checked fresh consent. These observations cover same-context Refresh, source-edit invalidation and capture recovery; a separate browser-focus test and every changed wallet/pool/round/terms combination are not claimed passed here.

At 2026-09-06 14:43:09 UTC, clicked `Submit round 1 proof` once. The request is `submit_checkin` to the existing core from B, with zero value, this pool ID, the exact statement above, `https://example.com`, the verified digest and an app-generated attempt UUID. The fifth argument is not exposed in the current visible form; its exact value must be recorded from the signed receipt, whose returned attempt ID links to storage. The public `get_attempt` record does not expose the nonce itself. Account, Refresh and proof controls disabled. The earlier capture-success toast/hash is not proof evidence. No proof transaction hash, verdict or successful round was claimed at this request checkpoint.

After the human replied `done`, the app initially showed acceptance for the distinct hash `0x3c28fa2643b48421dcaacd73cf044299fcb9645406399523d076cd7474899d06`. At 2026-09-06T14:48:37.860Z, independently verified FINALIZED status, successful execution, zero value, exact B sender and core target, all four known proof arguments and the actual fifth argument `af767cc3-0453-4eda-a854-0953b147cee3`. The complete calldata and return-code-0 payload are retained in the JSON. The actual verdict is `pass`, round 1 / attempt 1, participant status `success`, 1/1 rounds passed and `retriable = false`.

At 2026-09-06T14:50:16.253Z, finalized public reads confirmed the stored attempt and participant agree with the receipt. The complete proof hashes to `1e0300e5217047d1bfd1bac0dc207bd1b28edbfdaca78a9dc30d41b3e1f5e5b4`; both expected and observed source digests equal the reviewed capture digest. The stored reasoning supports both fixture facts, with recorded confidence 95 and provenance `leader_output_non_authoritative`; the full reasoning is retained, not treated as an independent audit. The exact nonce is retained in signed calldata, while the returned attempt ID, player, round, proof/evidence digests, verdict and timestamp link it to public attempt storage. No direct stored-nonce comparison is claimed.

Stored `submitted_at = 2026-09-06T14:47:18.018891+00:00` matches the signed receipt (`1788706038` Unix), inside the unchanged stored round `[1788704114, 1788707714)`. Finality at Unix `1788706096.0231538` also preceded closure. The pool remains active with unchanged terms and 200 wei staked. B is successful; A remains active at 0/1 with no attempt. `can_settle` is false and B's withdrawable credit is still zero. The app shows the same successful finalized proof, B's completed round and pass verdict, removes B's proof form, and re-enables account/Refresh controls. A read-only probe initially had a syntax typo and ran no RPC calls; its corrected verification passed without repeating the transaction.

## Switch to A and fresh login

After the human replied `done` to the account switch, at 2026-09-06 14:53:37 UTC the app showed `Wallet or network changed. Sign in again to continue.` B's signed-in label, participant view and credit had cleared without a reload; withdrawal was disabled and credit showed `Connect wallet to view`. This independently verifies the reset, not yet A's authenticated identity. At 14:53:52 UTC, clicked `Sign in with wallet` once. The app showed `Checking wallet session…` with sign-in and Refresh disabled. No transaction was opened, and the MetaMask extension was not inspected or controlled.

After the human replied `done` to the login signature, at 2026-09-06 14:55:48 UTC the app showed `Signed in · 0xab99…bad4` and `0xAb99…baD4 · you` in the exact pool. A is active at 0/1 rounds with 0/3 attempts and zero credit; B appears only as a public cohort member with 1/1 rounds and success. A's proof text and source URL were empty, with no carried-over B capture or consent. This verifies the authenticated app label and participant context; A's exact full sender will additionally be verified in the actual capture receipt. No authentication secrets were inspected or recorded.

## Wallet A public-source capture

At 2026-09-06T14:56:35.750Z, read-only checks verified helper protocol v3, its binding to the existing core, the 6,000-byte normalized-source policy and `funds_accepted = false`. The pool remains active with unchanged terms and stored round boundaries; A still has no prior attempt and zero credit.

At 2026-09-06 14:56:43 UTC, filled the visible source URL with `https://example.com` and clicked `Capture source on-chain` once. The request is `capture("https://example.com", "8b6ed2c4-fb65-4c1d-8389-e90defbaba1a")`, from A to the existing evidence helper with zero value. The generated recovery ID was read from the visible recovery textbox. The app shows `Capturing…`, with account, Refresh and proof controls disabled. This captures public permanent text; it is not a proof verdict or additional stake. No capture hash or full source snapshot has been observed yet, and A's proof text remains empty.

## Wallet A proof draft and focus check

After the human replied `done`, the app showed successful finality for `0x36849798ec1bf247e2bb6a9a742205dd1f17674fc18ef2adadb8de91ce8103ed`. At 2026-09-06T14:59:56.386Z, independently verified exact A sender, helper target, both arguments, zero value, FINALIZED status, successful execution and return-code-0 payload. The full stored snapshot matches the decoded return, with capture time Unix `1788706687` matching `2026-09-06T14:58:07.179277+00:00`. Reviewed all 127 normalized UTF-8 bytes in the app and stored snapshot; recomputed SHA-256 matches `8c1e8564424fdb68b8b7bdff3e16173a2e3599e9b71620637251486c5c4d5ed6`. This is A's distinct signed capture and recovery ID, although its public source content matches B's fixture.

At 2026-09-06 15:01:11 UTC, filled A's proof with the same exact technical-fixture statement recorded above for B, reviewed the complete A capture and checked fresh source consent. The app showed the correct source/digest/recovery ID, checked consent and enabled `Submit round 1 proof`. No proof request was opened. A still had 0/3 attempts and 0/1 rounds; B remained successful. The complete before-focus baseline is recorded in `proofFormA` in the JSON.

After the human replied `done` to the explicit tab-switch-and-return instruction, at 2026-09-06 15:04:26 UTC the app was refreshing with A's draft, source, capture ID and checked consent intact; writes were disabled while loading. After loading completed, all were unchanged and submission re-enabled. At 15:05:14 UTC, clicked the app's `Refresh` once and observed the same preservation and temporary write blocking through completion. This combines the human's reported focus action with independently observed before/after UI state; no hidden browser focus telemetry or every possible context-change combination is claimed.

At 2026-09-06 15:06:19 UTC, clicked `Submit round 1 proof` once. The request targets the existing core from A with zero value, this pool ID, the exact prepared technical-fixture statement, `https://example.com`, the reviewed digest and an app-generated fifth-argument UUID. The app disabled account, Refresh and proof controls. The old capture-success toast was not treated as proof evidence. No proof hash or nonce was observed before the interruption. The preceding `done` was for the focus test; no separate proof-approval reply is recorded before the interruption.

## Recovery after computer sleep

On resuming at 2026-09-07 03:32:15 UTC, the existing Chrome tab displayed `Your connection was interrupted`, `Your computer went to sleep` and `ERR_NETWORK_IO_SUSPENDED`. Warned against approving/retrying old wallet prompts and did not resend the proof. Restored the page through its visible `Reload` button; it ultimately showed `Sign in to continue`. No specific cause for that signed-out session is asserted.

At 2026-09-07T03:33:47.304Z, finalized public reads showed **both A and B successful, each with 1/1 rounds complete**. A's stored round-1 / attempt-1 verdict is `pass`, with submitted time `2026-09-06T15:07:22.208816+00:00`, inside the stored `[1788704114, 1788707714)` round. Its proof digest equals the exact prepared statement's digest, and expected/observed evidence digests both match A's reviewed capture. The complete attempt is retained in the JSON, including confidence 100 and the explicitly non-authoritative leader reasoning. This is verified stored state, not yet the recovered exact receipt/nonce or a claim that model confidence equals independent certainty.

The pool remains active and unsettled with unchanged terms and 200 wei staked; `can_settle` is true, and both current credits are zero. Both-pass expectations remain 100 wei each and zero fee, but no settlement output or payout is claimed. At 2026-09-07 03:34:25 UTC, clicked `Sign in with wallet` once for a fresh A login. The app showed `Checking wallet session…` with login and Refresh disabled. This is a signature-only request, not another proof, settlement or withdrawal.

## Next

After the human replied `done` to the recovery-login signature, at 2026-09-07 03:38:15 UTC the app showed `Signed in · 0xab99…bad4`, A's own successful participant marker and both wallets at 1/1 rounds complete. A's Activity recovered eight own requests, including existing proof hash `0xcc642df0c1ca3e9c6a99ec5183e6ad7b26eddfd9dd0525b65ec5579c5fc72c0b`. No B requests appeared with the optional current-wallet filter unchecked. After `Refresh saved history`, the same eight requests remained and the app explicitly confirmed that no transaction was resent. This is observed UI recovery/isolation, not exhaustive backend certification.

At 2026-09-07T03:40:10.227Z, independently verified A's recovered proof receipt: exact A sender, existing core, zero value, all four prepared arguments plus actual nonce `5dcd843a-9ab8-484c-b5e7-aeb1e499e59b`, FINALIZED status, successful execution and return-code-0 output. The returned round-1 / attempt-1 `pass`, successful participant state, reasoning and attempt ID match finalized storage. The exact proof/source digests and submitted time match as well. Finality at Unix `1788707284.912876` preceded stored closure `1788707714`. The full calldata, output payload and linked state are in the JSON. No duplicate proof or replacement source was submitted.

The same checkpoint verified both terminal successful participants, unchanged pool terms, 200 wei total stake, `can_settle = true` and zero current credit for A, B and the fee recipient. Settlement eligibility follows from both participants being terminal, without a local-clock assumption. The expected result is two winners, zero losers, zero forfeitures, zero fee, 100 wei credit per participant and a 200-wei conservation total; withdrawal is separate.

At 2026-09-07 03:40:53 UTC, returned to the exact pool and clicked `Settle & allocate credits` once. The pending request is `settle("human-wallet-60m-20260906-131207")`, from A to the existing core with zero value. Account, Refresh and settlement controls disabled. No settlement hash, actual allocation or withdrawal has yet been observed.

After the human replied `done`, at 2026-09-07T03:46:01.901Z independently verified settlement `0x5de046a70caed366bec2e4e8ecc69ae23c448128e4ba63fa09566f4e2e2ada34`: exact A sender, core target, pool-ID argument, zero value, FINALIZED status, successful execution and complete return-code-0 output. Stored `settled_at = 2026-09-07T03:42:31.330184+00:00` matches the signed receipt. Actual result: two winners, zero losers, zero forfeited pot, zero fee/share and a 200-wei conservation total. Both participant allocations and available credits are exactly 100 wei; the fee recipient's credit remains zero. The pool is settled with unchanged terms and `can_settle = false`.

Native-balance baseline before withdrawals: A `999999999999998880` wei, B `880` wei, core `2200` wei. The core also holds funds outside this trial; do not treat its entire balance as this pool's funds. The verified withdrawal scope is each caller's 100-wei credit only. The app shows `2 successful · 0 failed · 0 GEN fee` and A's exact `0.0000000000000001 GEN` available credit.

## Wallet A withdrawal request

An initial `Withdraw credit` browser click timed out at 2026-09-07 03:46:41 UTC; diagnostics showed the matched button disabled. Did not blindly repeat it. Reinspected the page, then A's complete Activity at 03:47:30 UTC: exactly nine saved requests, including settlement, with no new withdrawal request. The only withdrawal shown was the already-verified initial-pool refund. Reviewed the app's transaction path, which reserves its saved intent before opening `writeContract`. No product defect is inferred solely from this browser-control timeout.

Returned to the exact settled pool, rechecked A's enabled control and exact available credit, and at 2026-09-07 03:47:59 UTC clicked `Withdraw credit` once. The pending call is `withdraw()` from A to the existing core with **zero additional value**, requesting **100 wei (`0.0000000000000001 GEN`) back to A**. This is A's entire verified caller credit; no alternate recipient is selected. Account, Refresh and withdrawal controls disabled. The old settlement-success toast/hash is not withdrawal evidence. No parent hash, payout record or native delivery has yet been observed.

After the human replied `done`, the app displayed parent hash `0xba12f18e690c8429025cd34eca00082d6a02c47aa3cc4e24e271d90b2ee57efc`, the exact-delivery confirmation, zero current withdrawable credit and a disabled withdrawal control. The 100-wei settlement-credit label remains as the historical allocation, not available credit. No transaction was resent.

The independent verification initially stopped because the read-only harness expected an empty `args` array, but the receipt contained only `{ "method": "withdraw" }`. The shipped SDK intentionally omits an empty positional-argument array (`frontend/vendor/genlayer-js/index.js:228`). Corrected the verification assertion to require that exact no-argument object; no app code or transaction changed. This is a verification-harness correction, not evidence of a product failure.

At 2026-09-07T04:09:18.373Z, verified the exact A sender, existing core target, no arguments, zero additional value, FINALIZED status, successful execution and return-code-0 payload. The output exactly matches stored `payout-00000006`: recipient A, amount 100 wei, status `emitted_for_finalization`, and emission time `2026-09-07T03:49:48.970518+00:00`, matching the parent receipt. Parent finality is Unix `1788753024.072962`.

Separately verified linked native child `0xa5eaa5fcf74a3e5b44d085b0eb7b296df1ba70d4350868140aa8187cf376f118`: type 0, FINALIZED, `value_credited = true`, exact core sender, exact A recipient and exactly 100 wei. The parent names this child and the child names this parent. The child's creation time is `2026-09-07T03:50:24.090503+00:00`; a separate finality-monitoring timestamp was not exposed and is not invented. The stored payout's emission-only status is not itself the proof of delivery.

Finalized reads show A credit 0, B credit 100, fee-recipient credit 0, and the same settled pool and terms hash. Native balances corroborate delivery: A increased by exactly 100 wei to `999999999999998980`, B remained `880`, and core decreased by exactly 100 wei to `2100`. The full allow-listed parent input/output, stored payout, native child, state and balance checks are in the JSON record. Core funds outside this trial remain out of scope.

Next, the human must switch this site's connected account to B on Studionet and reply `done`. B's fresh login and separately human-approved withdrawal will follow, with its own independent native-delivery verification. No B login or withdrawal has been opened. The two explicit boundary simulations passed, but no live early-activation rejection transaction was submitted; keep those coverage claims separate. The creation-transaction rejection check also remains incomplete. No code, deployment, access policy or Git remote was changed during recovery.

## Switch to B for withdrawal and fresh login

After the human replied `done` to the account switch, at 2026-09-07 04:12:26 UTC the existing tab showed `Wallet or network changed. Sign in again to continue.` A's signed-in label and private participant view cleared without a browser reload; credit showed `Connect wallet to view` and withdrawal was disabled. This verifies the observed reset, not B's authenticated identity or actual wallet chain. The configured Studionet banner alone is not wallet-chain evidence.

At 2026-09-07 04:12:56 UTC, clicked `Sign in with wallet` once. The app showed `Checking wallet session…`, with sign-in and Refresh disabled. The reviewed login path verifies the current wallet and Studionet chain, validates an origin/product-bound challenge, requests `personal_sign`, and verifies the returned session. The human must approve only B's login signature and reply `done`. No withdrawal was requested, no successful B login is yet claimed, and the MetaMask extension was not inspected or controlled.

After the human replied `done`, at 2026-09-07 04:15:17 UTC the app showed `Signed in · 0xe6e7…5c43` and `0xE6E7…5c43 · you` in the exact settled pool. B has 1/1 successful rounds and exactly `0.0000000000000001 GEN` withdrawable. A appears only as a public successful cohort member. This observes B's authenticated app context; the exact full B sender will additionally be checked against the actual signed withdrawal receipt. No authentication secrets were inspected or recorded.

## Wallet B withdrawal request

At 2026-09-07T04:16:18.644Z, independent read-only checks confirmed Studionet 61999, the unchanged settled pool/terms hash, A credit 0, B credit 100 wei, and fee-recipient credit 0. Native balances before B's withdrawal: A `999999999999998980` wei, B `880` wei, core `2100` wei. B's entire caller credit is exactly its agreed stake. The core's other 2000 wei remains outside this trial.

Rechecked B's visible sign-in, the exact settled pool, available amount and enabled withdrawal control. At 2026-09-07 04:16:29 UTC, clicked `Withdraw credit` once. The pending call is `withdraw()` from B to the existing core, with **zero additional value**, requesting **100 wei (`0.0000000000000001 GEN`) back to B**. Account, Refresh and withdrawal controls disabled. No B withdrawal hash, stored payout or native delivery has yet been observed, and the MetaMask extension was not inspected or controlled.

Await `done` for B's withdrawal approval only; keep B selected. Independently verify the exact parent call and successful finality, matching stored payout, zero remaining credit for both wallets, and the separate finalized, credited native child delivering exactly 100 wei from core to B. Then reconcile total delivered stakes and save/export the private evidence. Creation-transaction rejection and other explicitly incomplete checklist items remain unpassed; no public submission or access change is authorized.

## Wallet B delivery and final reconciliation

After the human replied `done`, at 2026-09-07 04:18:48 UTC the app displayed `Payout delivered`, parent hash `0xc3cab67b9d832724e2c7b9a054f5641321864b907381a93f846a594e80f7b6ce`, zero current available credit and a disabled withdrawal control. At 2026-09-07T04:20:08.647Z, independent checks verified exact B sender, existing core target, no arguments, zero additional value, FINALIZED status, successful execution and the complete return-code-0 payout. The exact decoded calldata is `{ "method": "withdraw" }`; the shipped SDK intentionally omits empty arguments.

The returned payload exactly matches stored `payout-00000007`: recipient B, amount 100 wei, emission-only status and `emitted_at = 2026-09-07T04:17:33.937719+00:00`, matching the receipt. Parent finality is Unix `1788754688.6679296`. Separately verified native child `0x77008c190cd4c0e678540b809c578ba4f5d5a0b850728a0382bbda629029a07a`: type 0, FINALIZED, `value_credited = true`, exact core sender, exact B recipient and exactly 100 wei. Parent/child links match in both directions. The child was created at `2026-09-07T04:18:08.687982+00:00`; a separate finality-monitoring timestamp was not exposed and is not inferred.

Rechecked A's independently verified native child as well. Both caller credits and the fee recipient's credit are zero; the same pool remains settled with unchanged terms. Native balances after both withdrawals are A `999999999999998980` wei, B `980` wei and core `2000` wei. Each wallet increased by exactly 100 wei from the pre-withdrawal baseline; the core decreased by exactly 200 wei. Total staked 200 = delivered to A 100 + delivered to B 100 + fee 0, with zero remaining trial credit. The unrelated 2000 wei held by the core is unchanged. No transaction was resent.

## Final saved-history, reload and export checks

B's Activity showed six own saved requests: five from this retry and the prior underfilled-pool activation. The optional `Current wallet only` checkbox was unchecked; no A request appeared. B's withdrawal showed `Payout delivered`, the exact recipient/amount and the same finalized native-child hash. At 04:20:49 UTC, requested `Refresh saved history`; the same six requests returned and the app stated that no transaction was resent.

Used the visible export controls to download B's Activity at `2026-09-07T04:21:49.312Z` and the settled pool record at `2026-09-07T04:22:35.321Z`. At `2026-09-07T04:24:28.986Z`, parsed the actual downloaded files and verified them against this trial's independent evidence:

- `C:/Users/user/Downloads/activity-page.json`: 12,825 bytes, SHA-256 `027b0868f4c365441135ea1f8705d82c364cbc36af7a1dca0f895a002f0bff45`. Six B-only saved requests; all five current B receipt hashes, exact inputs and successful finality match; the exported payout matches the separately verified 100-wei native delivery.
- `C:/Users/user/Downloads/human-wallet-60m-20260906-131207-record.json`: 12,287 bytes, SHA-256 `9d3dbeb1315c0567abacdef50eb1e97f728031d4a3c2ff8727dceb7b5b507d4b`. Stored pool and both participant records match finalized verification. All seven pool-scoped core calls have matching hashes, exact inputs and successful finality. Helper captures and account-scoped withdrawals are separate and retained in the full trial JSON; the pool export explicitly does not claim a complete blockchain archive.

At 04:25:19 UTC, reloaded the exact pool URL. B's session restored without another signature, and the same settled pool and zero current credit returned. By 04:26:08 UTC, the reloaded Activity again showed the same six B-only requests and the same delivered payout/child. This is observed B recovery plus the earlier recorded two-way isolation checks, not an exhaustive authorization audit or a final post-withdrawal A Activity export.

## Remaining coverage and UI follow-up

- Creation-transaction rejection remains unobserved: the initial intended rejection was actually approved. Do not mark the unsigned-cancellation path passed.
- Changed-pool, changed-round and changed-terms consent-reset combinations have not each been human-observed. The recorded account/network reset, unchanged-context focus/Refresh and source-edit re-consent checks remain valid within their scope.
- Early-activation rejection was verified only through explicit stored-chain-time boundary simulations, not a live rejected transaction.
- Exact Chrome and MetaMask versions are still unrecorded. A final post-withdrawal A Activity reload/export is also not claimed at this checkpoint.
- UI follow-up `CP-HUMAN-UX-01` (low): after B's verified delivery and again after reload, the next-step guide still says `Review your allocation, then withdraw`. The current credit is correctly zero and withdrawal is disabled, so this is misleading completion guidance, not an observed duplicate payout. Distinguish available credit, pending delivery and completed delivery, and make historical allocations clear. No code was changed during this trial.

This evidence-only milestone is saved locally; no new Git push, CI trigger, deployment, visibility change or submission was performed. The automated clean suite was not rerun for this documentation checkpoint. Full human acceptance and public submission remain separate from this passed two-wallet happy path; public evaluator access and submission are deferred by the owner.

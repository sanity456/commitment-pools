# Commitment Pools — local UI verification

Scope: private, local-only changes after `cd79acc2c7216231d1cba810a2ef32d390e01f58`. This working-tree record is not immutable submission evidence, a deployed release, or a completed human-wallet test. Dispute Court was not changed.

## Changes

- Retained the approved emperor-penguin/vortex artwork and name-above-logo lockup. Kept the black and royal-gold theme.
- Shortened the home introduction and creation/owner headings. Moved the four-step explanation into a native disclosure; retained stake, refund, fee and failure information.
- Added explicit signed-out directory, workspace and Activity states. Directory reads have loading/error/retry states, concise result counts and separate index-coverage details.
- Made zero-credit withdrawal guidance neutral: “No credit to withdraw,” with payout verification in Activity. Positive credit is explicitly wallet-wide. Missing, malformed, stale, mismatched-wallet and in-flight credit cannot produce a payable/zero-credit assertion.
- Added a workspace shortcut to Activity; stopped indefinite signed-out pool/authority loading messages and unverified “no scheduled fee change” claims.
- Section changes return to the top. Main navigation has an accessible name; mobile tabs retain their horizontal scrolling. Form inputs use 16px text.

No contract source, chain-time rules, transaction methods, signing policy, dependency pins, deployment configuration or access controls were changed. No wallet request or on-chain transaction was initiated during the original UI editing/guest QA pass. The subsequent human login and read/export follow-up is recorded separately below. No commit, push or deployment was performed.

## Automated verification

- Complete frontend suite: **121 passed, 0 failed**, including seven new credit-guidance tests and the existing authorization, recovery, consent, payout and contrast tests.
- Final strict lint, formatting, both TypeScript configurations, Next/Vercel production build and Sites/Vinext production build: **passed**. Existing middleware-deprecation and Vinext route-classification notices remain non-blocking; dependency versions were not changed.
- Contract/direct/simulator suites and GitHub Actions were not rerun for this UI-only working tree. Earlier results must not be presented as CI evidence for these edits.

## Observed browser checks

Local Next/Vercel development preview: `http://127.0.0.1:4195/`. Inspected with the supported in-app browser, signed out, on September 7, 2026 (UTC).

| Check | Observed result |
| --- | --- |
| Desktop home at 1365 × 900 | Approved logo loaded; shorter introduction, primary controls and sign-in directory state visible. Document width 1350px, matching its client width; no page-level horizontal overflow. |
| Mobile home/workspace at 390 × 844 | Document/client width both 375px; labels and controls fit. Pool lookup stays beside its Open button. |
| Narrow home/create at 320 × 812 | Document/client width both 305px. Creation fields, including formation length/unit, remain within the page. Form input text measured 16px. |
| Creation form | Filled an unsent local title and changed formation length/unit to 15 minutes. Publishing stayed disabled while signed out; no wallet opened or record created. |
| Home explanation | Collapsed initially; opening “How pools work” revealed all four steps and the refund/failure/fee details. |
| Section navigation | Home and owner navigation returned `scrollY` to 0 after a scrolled form. |
| Workspace/Activity | Withdrawal disabled while signed out. View Activity opened an explicit sign-in state, not an endless loading message. |
| Direct invitation | Pool lookup navigated to `/pools/human-wallet-60m-20260906-131207`; it showed the sign-in requirement rather than falsely claiming to load protected details. |
| Owner console | Owner verification and fee actions remained disabled. Missing authority/configuration was not represented as loaded data. |
| My work | Explicit wallet sign-in state; no sample or private pool records shown. |
| Browser diagnostics | No error or warning entries returned by the inspected local tab. |

These are desktop-browser viewport checks, not a physical-phone wallet test or complete accessibility audit. No authenticated directory requests, populated pool screens, owner proof, keyboard-only journey or new native payout were human-tested in this pass. The credit cases above are automated presentation tests, not evidence of a new withdrawal.

## Remaining before acceptance

1. Review this exact UI on the protected preview after a separately authorized release; keep it private.
2. Finish the unobserved changed-pool/round/terms consent combinations and record exact Chrome/MetaMask versions.
3. Review and explicitly approve any deliberate retry after the earlier unsigned cancellation. Do not silently reuse or submit that prospective pool.
4. Only when submission is authorized: freeze an immutable commit, rerun the complete pinned Ubuntu workflow, recheck deployed source hashes and reviewer access, and complete the evidence response. Public access remains deferred.

The separate human-wallet checklist and historical evidence remain authoritative for observed transaction outcomes.

## Authenticated local follow-up

The [Wallet A follow-up](human-wallet-a-final-2026-09-07.md) subsequently passed on `http://localhost:4195` in the user's Chrome browser, after the user's login signature. The canonical local hostname resolved the earlier exact-origin rejection without weakening authentication or changing source/configuration files.

The settled pool showed fresh `0 GEN`, a disabled withdrawal button and `No credit to withdraw` before and after reload. A's historical 100-wei settlement allocation remained separate from current credit; Activity showed the already verified delivered payout and linked finalized native child. A's ten private requests persisted through saved-history refresh and reload, without requiring another signature. Actual Activity and pool downloads matched the prior independent receipts and stored settlement in the offline comparison at `2026-09-07T07:55:29.499Z`.

This closes the local authenticated `CP-HUMAN-UX-01` and A-history/export checks, not protected-preview release QA or the full human test. No new transaction was sent. The inspected app error log was empty; wallet content-script liveness/listener warnings were observed and retained in the follow-up record. Earlier guest-viewport observations and test results above remain limited to their original scope; no full-suite rerun or CI for a new commit is implied.

# Private human wallet trial — 2026-09-06

Status: **formation-refund path passed; full two-participant lifecycle still incomplete**. Wallet A's entire 100-wei stake was independently verified as delivered back to A, with no fee and zero residual credit. Login/rejection, account/network invalidation and two-way populated Activity UI isolation were also observed. The 15-minute formation window expired before B joined, so no proof, model verdict or two-participant settlement pass is claimed. Keep this record, the repository and the preview private. The owner deferred public access and submission; no access-policy or submission changes are authorized by this trial.

Recorded at: 2026-09-06 11:34:38 UTC. This is an observation time, not a chain timestamp or a contract-deadline assumption.

- Preview: `https://commitment-pools-studionet-28cvvvtta-sanity3.vercel.app/`
- Local review baseline: `dbcc749e16d4fe09de778cd93086bd987ac66a4a`
- Deployed app source: `ace5d3f78968dc8bf06a333f2c65c1ee37baee6e`, as recorded in the September readiness report.
- Surface: the user's existing Chrome browser and wallet extension. Full browser/extension versions have not yet been recorded.
- Observed account labels: initially `0xe6e7…5c43`, then `0xab99…bad4` after the reported account switch and fresh login. These abbreviated UI labels are not fresh full-address verifications and must not be expanded into payment destinations.

## Observed so far

| Check | Evidence and scope |
| --- | --- |
| Rejected login | User reported rejecting the first request. The temporary test tab was no longer open when verification resumed, so its cancellation notice was not captured. Reopening the preview showed `Sign in to continue.` No completed human cancellation-copy check is claimed. |
| Approved login | User reported approving a fresh login message. The retained Chrome tab displayed `Signed in · 0xe6e7…5c43`, a Sign out link and enabled authenticated controls. |
| Reload/session restoration | Navigating to the same preview URL showed the initial session-check state, then restored the same signed-in account and enabled controls without another login action from the assistant. |
| Unsent draft prepared | Opened Create and filled only Pool title with `Private wallet-switch test — UNSENT`. Did not select Review or publish. The other fields are still defaults, not approved pool terms. |
| Reported network switch — not yet verified | User replied `switchad`. Inspected the retained tab without navigating or reloading: it still showed `Signed in · 0xe6e7…5c43`, the unsent title and an enabled Review button. At approximately 2026-09-06 11:44:35 UTC, clicked the app's Refresh button (not a browser reload). Review was disabled during loading, then enabled again; the same signed-in label and draft remained. The wallet's active network for this site was not independently visible, so this is not a passed network-change lock/reset test. No conclusion about wrong-network transaction safety is drawn from the displayed state alone. |
| Network-label clarification | User then reported `ethereum`; a fresh retained-tab inspection still showed the same signed-in label and unsent title. At 2026-09-06 11:48:51 UTC, the site-specific network had not yet been established. MetaMask's [official network-switch instructions](https://support.metamask.io/configure/networks/how-to-change-networks/) distinguish a portfolio filter from each website's network connection. That is a possible explanation, not a verified cause. The earlier assistant suggestion of a confirmed app bug was premature. The code already rechecks account and chain during app Refresh and before submission; no runtime code change or deployment was made on this evidence alone. |
| Reported site-specific switch — unresolved | User replied `site switched`. At 2026-09-06 11:54:52 UTC, inspected the retained app tab without navigation, browser reload or a manual Refresh click. It initially showed loading with Review disabled, then returned to the same signed-in label, unsent title and enabled Review button. The lock/reset outcome was not observed. Attempted read-only inspection of the user's open MetaMask tab was blocked by the browser's URL policy; no alternate route was attempted. Request a user-provided screenshot of this exact site's connection panel showing its URL and selected network before attributing the mismatch to app code or test setup. No signature or transaction was requested. |
| User-provided screenshot clarified the control | The supplied `Screenshot 2026-09-06 045732.png` showed `Network: Ethereum` in the token-list filter and a separate `G` dropdown in the bottom connection bar next to the truncated Commitment Pools site label. Directed the user to that site-specific dropdown; did not inspect or control the blocked extension through another route. |
| Site network-change sign-out/reset — passed for the observed UI | After that instruction, the user replied `switched`. At 2026-09-06 12:00:02 UTC, the retained app tab displayed `Wallet or network changed. Sign in again to continue.`, sign-in buttons instead of the account/sign-out controls, and reset to Explore. No browser navigation, reload or manual app Refresh was performed. Opened Create: Pool title was empty, the `Private wallet-switch test — UNSENT` marker was absent, and `Review pool before publishing` was disabled. No signature or transaction was requested. This closes the observed network-change sign-out/draft-reset check, not account-switch acceptance or an on-chain wrong-network transaction test. No runtime fix or redeployment was necessary to obtain this result. |
| Return to Studionet — old session stayed signed out | User replied `back` after being asked to return the same site connection to Studionet. At 2026-09-06 12:03:15 UTC, the retained app still displayed `Wallet or network changed. Sign in again to continue.`, an empty Pool title and a disabled Review button. Returning did not visibly restore the old session. Clicked `Sign in with wallet` once to request a fresh human login; the app entered its pending sign-in state. Approval and successful session restoration are not yet observed for this request. No transaction was initiated. |
| Fresh login after network return — passed | User replied `approved`. At 2026-09-06 12:05:43 UTC, the retained app showed `Signed in · 0xe6e7…5c43`, the Sign out link and enabled authenticated controls. This verifies the fresh login following network invalidation; it does not independently verify a full wallet address. |
| Account-switch draft prepared | Opened Create and filled only Pool title with `Private account-switch test — UNSENT`. The same signed-in label and enabled Review button remained visible. Did not click Review or publish; all other fields remain defaults, not approved pool terms. The next check is to change the account connected to this exact site while leaving its network on Studionet. |
| Account-change sign-out/reset — passed for the observed UI | User replied `account switched`. At 2026-09-06 12:09:02 UTC, the retained app displayed `Wallet or network changed. Sign in again to continue.`, sign-in controls instead of the previous account/sign-out controls, and reset to Explore. No navigation, reload or manual Refresh was performed. Opened Create: Pool title was empty, `Private account-switch test — UNSENT` was absent, and `Review pool before publishing` was disabled. This verifies the observed invalidation/reset after the reported account change, not the new account's identity or full private-data isolation after login. |
| Rejected-login capture retry — pending | Clicked `Sign in with wallet` once after the account-change check to request a fresh login message, intending for the user to reject it. The retained app entered its pending sign-in state with login and Review disabled. This repeat is needed because the first rejected-login cancellation notice was not captured. No completed rejection is claimed yet and no transaction was initiated. |
| Rejected-login capture retry — passed | User replied `rejected`. At 2026-09-06 12:10:50 UTC, inspected the retained app without navigation or reloading. It displayed `Wallet request cancelled. Nothing was sent.`, sign-in controls instead of an authenticated account/sign-out link, an empty Pool title and a disabled `Review pool before publishing` button. Sign-in controls were enabled again, allowing a deliberate retry. This closes the earlier cancellation-copy observation gap. The rejected login has no transaction hash and no on-chain action was initiated. |
| Fresh login with changed account — passed | User replied `Approved.` to the deliberate retry following the verified rejection. At 2026-09-06 12:13:00 UTC, the retained app showed `Signed in · 0xab99…bad4`, a Sign out link and enabled authenticated controls. The displayed account differs from the prior `0xe6e7…5c43` label. Opened Create: Pool title remained empty and neither earlier unsent marker reappeared. No new login request was initiated during this verification. |
| Changed account's private-view smoke check | Activity completed loading with `No saved requests yet` and `0 saved requests`. My work completed with `0 matching records`, with automated fixtures still hidden. Help & settings displayed `No support requests have been saved.` No preferences or support requests were written. These empty-state observations do not by themselves prove cross-account isolation of populated records; repeat that check once the human lifecycle generates distinct private activity for both wallets. |

## Trial chronology

The following entries preserve what was observed at each step, including earlier pending states. The current outcome is summarized above and in the final withdrawal entry; no wallet request remains pending.

The owner authorized starting the described tiny Studionet lifecycle test and requested `done` as the completion reply for subsequent human steps. Interpret it only as completion of the most recently requested step, not automatic success or blanket signing authority. Repository and preview access remain unchanged.

At 2026-09-06 12:28:55 UTC, requested the first zero-value `create_pool` transaction through the app, initially intending deliberate rejection. The user instead approved it and explicitly clarified `im the one that accepted it`. Record this as approved creation, not a passed transaction-rejection test. No duplicate creation was opened. Pool ID: `human-wallet-20260906-122644`. The reviewed source-verified rules specify 100 wei per participant, a 2/2 cohort, 15-minute formation, one one-hour round and a 5% forfeiture fee. The public source is [Example Domain](https://example.com), a technical page-verification fixture, not a real-world work or identity claim.

The app's Activity displayed `Execution succeeded and finalized` for `0x7d2fae96b129225e617226007823cc71b6147ea11fdd34a68039fe2b58100763`. Independent public RPC verification passed: sender `0xAb99c741494bEF91FAE66144dda31Be93180baD4`, expected v3 core target, zero value, all eleven decoded arguments, `FINALIZED`, execution success, and result code `0` (`return`). The exact calldata, returned payload and receipt metadata are retained in the lifecycle JSON. The pool's stored creation timestamp is `2026-09-06T12:29:21.028003+00:00`; stored formation deadline/activity start is `1788698661` (`2026-09-06T12:44:21Z`), and activity end is `1788702261` (`2026-09-06T13:44:21Z`). Before the funded join request, `LATEST_FINAL` reads returned `can_join: true`, zero participants and zero contract credit for both supplied actors.

Opened wallet A's `join` request for exactly **100 wei** through the existing pool workspace. The matching A account label was still signed in. Reviewed the immutable terms, checked the exact-stake consent box and clicked the stake action once; the app entered its pending state. Clearly instructed the human to **approve**, not reject, this prompt. The request was opened between observation clock reads at 12:37:49 and 12:39:09 UTC. No successful join or join hash is claimed yet.

The read-only live release check passed again (chain/version, finalized-success deployments, exact source bytes, current 500 bps fee and helper linkage). Public balance reads for the two previously supplied actor addresses returned enough test GEN for the proposed stake; these reads do not independently identify the account in MetaMask. Exact prepared inputs, preflight outputs and limits are saved in [the lifecycle record](human-wallet-lifecycle-2026-09-06.json).

### Wallet A join — verified

User replied `done` to the pending stake request. At 2026-09-06 12:42:30 UTC, the app showed `Join pool executed successfully and finalized on Studionet`, one participant and wallet A active with 0/1 rounds passed. Public receipt `0xb57be739701dd784e2d2d2afb5492f2d9e77ab0e370abf648a0367fe40e4d51f` independently verified the exact A sender, core target, pool ID, 100-wei value, `FINALIZED`, execution success and result code `0` (`return`). Stored `joined_at` is `2026-09-06T12:39:16.740331+00:00`, which precedes the stored `2026-09-06T12:44:21Z` join deadline. The finalized pool read showed one participant and 100 wei total staked; `can_join` remained true. Exact receipt, output and participant/pool snapshots are in the lifecycle JSON. Requested switching this site's connected account to B while keeping Studionet selected; no B signature or funded join has yet been requested.

### Switch to B and expired formation window

The user replied `done` to the site-account switch. The retained tab displayed `Wallet or network changed. Sign in again to continue.`, removed A's signed-in label, showed `Connect wallet to view` instead of credit, and disabled withdrawal. No reload or navigation was used to obtain this state. A public `can_join` read still returned true immediately before the stored deadline. Clicked `Sign in with wallet` once; at 2026-09-06 12:44:23 UTC the app was `Checking wallet session…`, awaiting the human login signature. B's authenticated identity is not yet verified.

At 2026-09-06 12:44:59 UTC, a fresh `LATEST_FINAL` public contract read returned `can_join: false`. The 15-minute formation window was too short for this guided approval flow; B's stake was not requested. This is not a completed two-participant lifecycle. Do not duplicate the pool or request a late funded join. The verified v3 source provides the recovery path: `activate_pool` selects `minimum_cohort_not_met` during the activation grace period for the observed one-of-two cohort; A can subsequently claim the full 100-wei formation-refund credit and request withdrawal. These recovery outcomes are expected, not observed. A longer formation window should be agreed before creating a replacement.

### B login, private Activity isolation and refund-enabling request

After the user replied `done` for the login signature, the retained app displayed `Signed in · 0xe6e7…5c43`, the Sign out link, zero credit and `Not a participant`. The expired join control was absent; `Activate or enable refunds` was available. Opened Activity and waited for loading to finish: `No saved requests yet`, `0 saved requests`, with `Current wallet only` unchecked. Neither of A's prior saved lifecycle requests appeared. This passes the observed A-to-B private Activity UI check, not every backend authorization path or reverse populated-record isolation. A's public on-chain participation remains visible by design.

At 2026-09-06 12:47:34 UTC, fresh finalized-state public reads confirmed the pool still forming, one participant, minimum two, 100 wei total staked and `can_join: false`. Reviewed the source path: `activate_pool` with the pool ID and zero value enables refunding for an underfilled cohort; it does not itself credit or deliver the refund. Returned to the same pool and clicked `Activate or enable refunds` once. At 2026-09-06 12:48:13 UTC, the action, account button and Refresh were disabled in the pending state. Asked the human to approve this zero-value B transaction. No B deposit, completed activation, refund credit or delivered payout is claimed. Verify the full B sender, method, arguments, value, execution and resulting reason code from its receipt before continuing.

### Refunds enabled — verified

The user replied `done` for B's zero-value request. The app initially showed `accepted` and kept the action disabled; no duplicate was submitted. At 2026-09-06 12:52:06 UTC, public receipt `0x5e3e2c2afdabcc2be134aa68f586f7cf85ed542dde6fc6fa19c15bb191e6c327` was independently verified as finalized and successfully executed: exact B sender `0xE6E7bfFA242d2900fad7067012564d441F735c43`, expected core, `activate_pool`, the existing pool ID, zero value and result code `0` (`return`). Stored transaction time `2026-09-06T12:50:43.788563+00:00` falls after the stored join deadline and before the stored activation-grace end. Finalized pool state is `refunding` with `activation_failure: minimum_cohort_not_met`, one participant, 100 wei total staked and zero refund claims. A's credit remains `0`; no refund has been credited or delivered yet.

At 2026-09-06 12:52:15 UTC, B's Activity completed loading with exactly one saved `Activate pool` request and `Execution succeeded and finalized`, matching that hash. `Current wallet only` was unchecked. A's creation and join were absent. Both accounts now have distinct saved requests for the reverse isolation check. Requested switching this site's connected account back to A on Studionet. No new signature or transaction was requested in this verification turn.

### Return to A — fresh login requested

The user replied `done` to the switch back to A. At the 2026-09-06 12:54:09 UTC inspection, the retained app displayed `Wallet or network changed. Sign in again to continue.` B's signed-in label and populated Activity view were no longer displayed; the workspace showed `Connect wallet to view` and disabled withdrawal. No browser reload or navigation was used. Clicked `Sign in with wallet` once. At 2026-09-06 12:54:25 UTC, the app displayed `Checking wallet session…` with login and Refresh disabled. A's fresh authentication is pending; no refund claim or withdrawal was requested.

### A login and reverse isolation verified; full-refund claim requested

The user replied `done` for A's fresh login. At the 2026-09-06 12:55:39 UTC inspection, the app displayed `Signed in · 0xab99…bad4`, the pool in `refunding`, `Activation outcome: minimum cohort not met`, zero credit and an enabled `Claim full refund credit`. Fresh finalized-state reads at 12:56:14 UTC verified A's 100-wei participant record with `refund_claimed: false`, pool `refund_count: 0` and A credit `0`.

Opened Activity and waited for loading to finish. At 12:56:32 UTC, A saw exactly its two finalized successful requests (creation and join); B's finalized activation was absent, with `Current wallet only` unchecked. Together with B's prior isolated Activity view, this closes the observed two-way populated Activity UI check for these test records. It is not a comprehensive authorization/privacy certification.

Returned to the same pool and clicked `Claim full refund credit` once. At 2026-09-06 12:56:45 UTC, the claim, account button and Refresh were disabled in the pending state. Asked the human to approve A's zero-value `claim_formation_refund` call for the existing pool. Expected output is a full 100-wei credit and pool cancellation after its only participant claims. This expectation is not a verified credit or payout; withdrawal remains a separate later request.

### Full refund credit verified; withdrawal requested

The user replied `done` for A's refund claim. The app showed `Claim formation refund executed successfully and finalized on Studionet`, a cancelled pool, A marked refunded and `0.0000000000000001 GEN` withdrawable credit. At 2026-09-06 13:00:08 UTC, public receipt `0xe1163f498178095cd8990c6e1673fd41b7b2e67ef955c6afaf2d8d0cc3f0e2ea` independently verified the exact A sender, core, `claim_formation_refund`, pool ID, zero value, successful finalization and result code `0` (`return`). Exact output: `credit_wei: 100`, existing pool ID and `status: cancelled`. Finalized reads verified `refund_count: 1`, participant `refund_claimed: true`, settlement credit 100 wei, A contract credit 100 wei and fee zero. Stored pool `settled_at` matches the receipt's `2026-09-06T12:57:32.268180+00:00` timestamp.

Before withdrawal, A's public native balance was `999999999999998880` wei; a 100-wei delivery would restore `999999999999998980` wei, the recorded pre-stake balance. A balance difference alone will not substitute for verification of the linked native child transaction. Reviewed the exact 100-wei credit and clicked `Withdraw credit` once. At 2026-09-06 13:00:30 UTC, withdrawal, account and Refresh controls were disabled in the pending state. Requested human approval of this zero-additional-value transaction, expected to return the credit to its same caller A. No finalized withdrawal or native delivery is claimed yet.

### Withdrawal and actual native refund delivery — passed

The user replied `done` for withdrawal. At 2026-09-06 13:03:54 UTC, public receipt `0xdef3ccf4480417eb828847358a1edb3b48f5046e6b2baa57ebd3bc4204513e30` independently verified A's exact sender, expected core, `withdraw` with no arguments, zero additional value, successful finalization and result code `0` (`return`). Its output is `payout-00000005` for 100 wei to A. The payout record's `emitted_for_finalization` status was not treated as proof of delivery.

The parent identifies native child `0x0597d839bcffe4d40c7701e3b65e7e49553febc26d8d48e4cab3426896922412`. An independent read verified the child is type `0`, `FINALIZED`, `value_credited: true`, from the expected core to `0xAb99c741494bEF91FAE66144dda31Be93180baD4`, for exactly 100 wei. Its `triggered_by` matches the withdrawal parent and `triggered_on` is `finalized`. Stored parent time is `2026-09-06T13:00:52.129256+00:00`; stored child time is `2026-09-06T13:01:29.612222+00:00`. A's native balance increased by 100 wei to `999999999999998980` wei, matching the pre-stake balance. A's contract credit is zero.

The app displayed `Payout delivered`, the exact amount and recipient, and the same `Credited & finalized` child hash in A's saved Activity. Four A requests were present (creation, join, refund claim, withdrawal); B's activation remained absent. This closes the human formation-refund recovery path, not the intended two-participant source/proof/settlement lifecycle. No recovery or wallet action remains pending.

### Evidence checkpoint checks

At 2026-09-06 13:07:55 UTC, refreshing saved Activity completed with `Activity refreshed. This did not resend any transaction.` All four A requests and the delivered native child remained displayed. Local artifact-integrity assertions passed for five unique human contract transactions: each recorded encoded input and return payload decoded exactly to its retained JSON, every parent was finalized/successful, and the credited native child linked reciprocally to its parent with exact sender/recipient/100-wei amount. The final balance equals the recorded pre-stake balance and no transaction is pending. A read-only GitHub metadata check confirmed `sanity456/commitment-pools` remains private. No runtime source, deployment or access-policy change was made during this human trial.

## Next checks

- Agree a longer formation window before creating a replacement two-participant test with both joins, source captures, proof submissions, actual verdicts, settlement and native payouts. Proposed retry window: 60 minutes instead of 15, with all other reviewed terms unchanged; this proposal is not approval to create another pool.
- Preserve this successfully completed underfilled-pool refund record separately from the next trial. Do not duplicate any of its five completed contract calls or its native transfer.
- Creation-transaction rejection remains untested; the separate login-rejection check already passed. Do not duplicate creation or A's completed join.
- The two-way populated Activity UI isolation check passed for these test records. Record browser/extension versions before final acceptance; broader private-record/API coverage remains separate. No complete two-participant funded lifecycle is claimed.
- Complete the focus/refresh and changed-evidence consent checks on a real proof form during the next run. No proof form was reached in this underfilled pool.

No on-chain actions were initiated during the preliminary login checks. This trial completed five verified successful human-approved contract transactions and one independently verified native child transfer. The full 100-wei stake was delivered back to A. No B join, evidence capture or two-participant settlement occurred. No secret, login-signature payload, cookie, private key or seed phrase is recorded here; transaction calldata and outputs are public chain data. Repository/preview access has not changed.
